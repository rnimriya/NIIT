name: web
port: 3000
image: { repository: ghcr.io/rnimriya/neet-ai/web, tag: latest, pullPolicy: IfNotPresent }
envFrom: []
probes: { readiness: /, liveness: / }
autoscaling: { enabled: true, minReplicas: 2, maxReplicas: 8, targetCPUUtilizationPercentage: 70 }
